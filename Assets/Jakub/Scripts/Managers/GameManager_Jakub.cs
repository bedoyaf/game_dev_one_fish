using UnityEngine;

/// <summary>
/// Current state:
///     Global persistent data between application runs (Game Settings)
///     Persistent data per save slot (each different / default if none given) 
///         also saved between runs
///     Run data (changes between each run)
///     
/// 
/// </summary>
public class GameManager_Jakub : SmartSingleton<GameManager>
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // -------------------------------------------------------



}
